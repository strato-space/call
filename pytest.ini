[pytest]
# Keep default verbosity; callers can override with -q or -vv
addopts = -p no:hypothesis -p no:hypothesispytest
asyncio_mode = auto
markers =
    asyncio: mark test as asyncio and enable pytest-asyncio
filterwarnings =
    ignore:The global interpreter lock \(GIL\) has been enabled to load module '_cffi_backend':RuntimeWarning
