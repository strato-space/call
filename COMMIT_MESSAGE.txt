feat: add comprehensive logging for agents-as-tools and fix YAML formatting in MCP hooks

Enhance agents-as-tools wrapper with input/output logging similar to MCP Hook pattern,
improve YAML formatting for MCP tool results, fix prompt lookup to support YAML list-of-dicts,
and remove project filter constraints for helper agent resolution.

Changes in app/call.py:

1. Agents-as-Tools Logging Enhancement (lines 1096-1183)
   - Added input logging: [Agent Tool][{name}] Calling tool with YAML-formatted arguments
   - Added output logging: [Agent Tool][{name}] Tool returned with YAML-formatted results
   - Reuses _dump_yaml_literal for consistent formatting with MCP hooks
   - Pydantic models recursively converted to dicts before formatting
   - JSON input parsed and displayed as clean YAML
   - Fallback to raw display on parse errors
   
2. YAML Formatting Improvements
   a. _dump_yaml_literal (line 82)
      - Increased default width from 1000 to 10000 to prevent unwanted line wrapping
      - Added explicit line_break parameter for consistent newline handling
   
   b. _literal_yaml_str_representer (lines 68-76)
      - Removed problematic folded scalar (>) that was collapsing lines
      - Multiline strings use literal block scalar (|) for clean display
      - Strings starting with YAML special chars use quoted style (')
   
   c. _dump_like_mapping recursive conversion (lines 2713-2738)
      - Enhanced to recursively convert nested pydantic models
      - Processes dicts, lists, tuples, and sets recursively
      - Ensures all nested structures unwrapped before YAML serialization
   
   d. _unescape_strings conditional processing (lines 2743-2769)
      - Only processes strings containing escape sequences (check for backslash)
      - Handles: \n \r \t \" \' \\ with proper sequence
      - Uses \x00 as temporary marker to prevent double-processing
   
   e. _deep_unescape in MCP hook (lines 2906-2929)
      - Same conditional logic as _unescape_strings
      - Applied in call_tool method for MCP result formatting
   
   f. _to_yaml_text width update (line 2930)
      - Changed from width=1000 to width=10000 for MCP hook output

3. _collect_tools YAML List-of-Dicts Support (lines 962-972)
   - Fixed parsing of prompts defined as YAML list-of-dicts
   - Pattern: [{'PM-Router': 'desc'}, {'PM-1-DayStatusSummarizer': 'desc'}]
   - Checks if list items are dicts and extracts key-value pairs
   - Preserves backward compatibility with simple string lists
   
4. _build_agent_tool Project Filter Removal (line 1216)
   - Changed from project=project_scope to project=None
   - Allows target-based resolution without project constraints
   - Fixes SelectionNotFoundError for prompts stored as target entries

Changes in lib/repo_db.py:

5. _add_filter_clause Prompt Lookup Enhancement (lines 710-727)
   - Prompts now matched by both prompt column and target column
   - Query: (prompt = ? OR target = ? OR target = ?) COLLATE NOCASE
   - Enables lookup of YAML-defined prompts stored as standalone targets
   - Critical for PM orchestrator agents-as-tools resolution

Changes in mcp_config.yaml:

6. MCP Server Configuration (line 136)
   - Disabled call MCP server (enabled: false)
   - Prevents recursive MCP registration during runtime

Changes in app/tests/test_agents_tool_wrapper.py:

7. Test Enhancement (lines 11-164)
   - Added debug_print call capture to verify logging hooks
   - Tests verify [Agent Tool][{name}] Calling tool log
   - Tests verify [Agent Tool] Input (YAML) log
   - Tests verify [Agent Tool][{name}] Tool returned log
   - All assertions check for expected log patterns

Changes in README.md:

8. Documentation Update (lines 184-191)
   - Documented agents-as-tools comprehensive logging
   - Described input/output logging format
   - Referenced test_agents_tool_wrapper.py for verification
   - Explained YAML formatting pattern alignment with MCP Hook
   - Added details on disabling Telegram banners and debug logs

Impact:
- PM orchestrator now successfully loads all 11 helper prompts as tools
- PM-Router, PM-1-DayStatusSummarizer, PM-2-WeekStatus, etc. resolve correctly
- Agent-as-tool invocations now fully logged with input/output in YAML format
- MCP tool logs display clean, readable YAML without escape sequences
- File content from fs:read_text_file shows proper multiline formatting
- No more "Skip entry: error=No card found" for PM prompts
- All 153 tests pass

Technical Details:
- YAML list-of-dicts pattern [{'name': 'desc'}, ...] supported in attributes.prompts
- Target-based resolution works independently of project scope
- Escape sequence processing uses \x00 marker to prevent double-unescaping
- YAML literal block scalars preserve formatting for multiline content
- Pydantic model conversion happens before escape sequence processing
- Width=10000 prevents line wrapping for large YAML documents

Testing:
- test_agents_as_tools_wrapper_assigns_logging verifies all logging hooks
- 153 tests pass with 1 warning (pre-existing RuntimeWarning in trio mode)
- Verified with: python -m call.cli.main exec --target PM --input "test день"
- All PM helper prompts load successfully without SelectionNotFoundError
- MCP tool output formatting confirmed in debug logs

Files Modified:
- app/call.py: 3 major enhancements, 100+ lines added/changed
- lib/repo_db.py: 1 function enhanced, 22 lines added
- mcp_config.yaml: 1 flag toggled
- app/tests/test_agents_tool_wrapper.py: test enhanced with logging verification
- README.md: documentation updated with comprehensive logging details

Related:
- Follows prompt file standardization (METADATA + PROMPT sections)
- Supports build_runnable_instructions_config model precedence chain
- Compatible with repo_db/repo_fs refactor architecture
- Aligns with MCP Hook formatting patterns for consistency
